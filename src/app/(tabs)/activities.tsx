import { PlaceholderScreen } from '@/components/PlaceholderScreen';
import { tabScreenData } from '@/mocks/game';

export default function ActivitiesScreen() {
  return <PlaceholderScreen data={tabScreenData.activities} />;
}

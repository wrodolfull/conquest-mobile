const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');
test('50 meter influence rule has expected boundaries',()=>{const{influenceForDistance}=require('../.test-dist/features/activity/outdoorRules.js');assert.equal(influenceForDistance(49),0);assert.equal(influenceForDistance(50),1);assert.equal(influenceForDistance(100),2);assert.equal(influenceForDistance(500),10);assert.equal(influenceForDistance(1000),20)});
test('battle expected result distinguishes capture contested and pressure',()=>{const{expectedBattleResult}=require('../.test-dist/features/battles/battleRules.js');assert.equal(expectedBattleResult({myInfluence:8,leaderInfluence:9,influenceReward:2}),'captured');assert.equal(expectedBattleResult({myInfluence:8,leaderInfluence:10,influenceReward:2}),'contested');assert.equal(expectedBattleResult({myInfluence:6,leaderInfluence:14,influenceReward:2}),'pressure')});
test('avatar validation and ownership reject unsafe values',()=>{const{validateAvatarAsset,avatarObjectPath}=require('../.test-dist/features/profile/avatarRules.js');assert.equal(validateAvatarAsset({mimeType:'image/jpeg',fileSize:50}),'image/jpeg');assert.throws(()=>validateAvatarAsset({mimeType:'image/gif'}));assert.throws(()=>validateAvatarAsset({mimeType:'image/png',fileSize:6*1024*1024}));assert.equal(avatarObjectPath('https://x/storage/v1/object/public/avatars/me/avatar.jpg','me'),'me/avatar.jpg');assert.equal(avatarObjectPath('https://google.com/photo','me'),null);assert.equal(avatarObjectPath('https://x/storage/v1/object/public/avatars/other/avatar.jpg','me'),null)});
test('migration uses 25 percent presence, required influence, safe battle anonymization, and 50 objective',()=>{const sql=fs.readFileSync('supabase/migrations/202609260001_real_device_beta_findings_v1.sql','utf8');assert.match(sql,/min_presence_ratio numeric not null default 0\.25/);assert.match(sql,/'requiredInfluence',required_influence/);assert.match(sql,/on delete set null/);assert.match(sql,/if influence>=50/);assert.match(sql,/'target',50/)});
test('foreground tracking remains a single expo-location notification without a fake action',()=>{const source=fs.readFileSync('src/services/location/locationTrackingOptions.ts','utf8');assert.match(source,/foregroundService/);assert.match(source,/Your route is still being recorded/);assert.doesNotMatch(source,/notificationCategory|STOP/)});
